sudo: false

language: node_js
services: mongodb
node_js: 0.10.40

before_install:
- curl https://install.meteor.com | /bin/sh
- export PATH="$HOME/.meteor:$PATH"
- npm install -g spacejam
- meteor npm install
- meteor --version
- make decrypt

script: make test
